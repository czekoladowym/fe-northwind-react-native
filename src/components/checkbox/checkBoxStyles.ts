import styled from "styled-components/native"

export const MainField = styled.View`
		justify-content: flex-start;
		align-items: center;
    flex-direction: row;
    flex-wrap: wrap;
`

export const Title = styled.Text`
		font-size: 16;
		color: #000;
		margin-left: 8px;
    font-family: 'SegoeUI';
`